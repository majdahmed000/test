#!/bin/bash

# =========================
# Variables (edit here)
# =========================
NAMESPACE="monitoring"
DOMAIN="grafana.llm-k8s.awssolutionsprovider.com"
SERVICE_NAME="grafana"
SERVICE_PORT=80
PARENT_NAME="traefik"
PARENT_NAMESPACE="traefik"
ROUTE_NAME="grafana-route"

# =========================
# Apply HTTPRoute
# =========================
cat <<EOF | kubectl apply -f -
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ${ROUTE_NAME}
  namespace: ${NAMESPACE}

spec:
  parentRefs:
    - name: ${PARENT_NAME}
      namespace: ${PARENT_NAMESPACE}

  hostnames:
    - ${DOMAIN}

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ${SERVICE_NAME}
          port: ${SERVICE_PORT}
EOF

echo "Grafana HTTPRoute applied successfully"
kubectl get HTTPRoute -n ${NAMESPACE}